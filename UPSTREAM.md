# Upstream provenance

Pinned pack: `K-Dense-AI/scientific-agent-skills` version `2.65.0`.

Commit: `1dd0fccf46fc3c9855c4a0c313a0c57fe4319883`.

This distribution bundles 163 modules from that pinned upstream revision, including the scientific `skills/pptx` module. It does not contain or replace an independent direct `/pptx` skill.

The module files and K-Dense license notice are preserved from the pinned source. This distribution does not auto-update; a later upstream version requires a separately reviewed release.
